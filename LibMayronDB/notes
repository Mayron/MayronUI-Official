trackerData = tracker/read-only/observer data


Current:
trackerData.rootData ...
	- changes
	- root -- this was used to save the change to the basic table!
	- observerPath
	- database
trackerData.data
trackerData.path
trackerData.children -- not sure if needed if all trackers are stored in allTrackerData

New:

trackerData.path
trackerData.observer -- can use observer:GetDatabase() if needed, and GetPathAddress()
trackerData.tbl
trackerData.tracker -- tracker needs access to tbl
trackerData.changes